import React from "react";

export default function DashboardPage() {
  return (
    <div>
      <h1>Dashboard</h1>
      {/* Add dashboard components such as profile cards, progress bars, etc. */}
    </div>
  );
}
